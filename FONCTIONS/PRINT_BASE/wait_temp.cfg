[gcode_macro WAIT_TEMP]
gcode:
    STATUS_HEATING
    SET_HEATER_TEMPERATURE HEATER=extruder TARGET={params.HOTEND}
    SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET={params.BED}
    TEMPERATURE_WAIT SENSOR=extruder MINIMUM={params.HOTEND}
    TEMPERATURE_WAIT SENSOR=heater_bed MINIMUM={params.BED}
    STATUS_READY